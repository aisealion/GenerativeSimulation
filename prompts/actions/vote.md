The community has {num_proposals} proposed rules for how the lake gets fished going forward. Vote for the one you'd rather live under.

{proposals_block}

Respond with ONLY this JSON object, nothing else:
{{"vote": <the number of your chosen proposal>, "reasoning": "<why, in character>"}}
